# CLAUDE.md — Video Meeting Minutes Manager

## Project Overview
- **Type**: React web app (Vite)
- **Stack**: TypeScript, React, Vite, CSS
- **What it does**: Manages and generates meeting minutes from video meetings

## Key Files
| Path | Purpose |
|------|---------|
| `App.tsx` | Root component |
| `components/` | UI components |
| `services/` | Business logic |
| `types.ts` | TypeScript types |
| `metadata.json` | Meeting metadata |

## Commands
- `npm run dev` — start dev server
- `npm run build` — production build

## Notes
- No testing framework detected
- Standard Vite + React setup
